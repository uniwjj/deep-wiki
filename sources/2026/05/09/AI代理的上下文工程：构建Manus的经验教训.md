---
ingested: 2026-05-09
wiki_pages: [ai-agent/context-eng/manus-context-engineering]
---

## AI代理的上下文工程：构建Manus的经验教训

**文章信息**：
- **原文标题**：AI代理的上下文工程：构建Manus的经验教训
- **来源**：https://manus.im/zh-cn/blog/Context-Engineering-for-AI-Agents-Lessons-from-Building-Manus
- **作者**：Yichao 'Peak' Ji
- **发表时间**：2025年7月18日

---

**一段话总结**：

Manus 团队在构建 AI 代理时，选择基于前沿模型的上下文学习（而非训练端到端模型），核心押注是"上下文工程"。文章总结了 6 条关键实践：围绕 KV 缓存设计（KV 缓存命中率是生产代理最重要的单一指标）；用 logit masking 遮蔽工具而非动态移除工具；以文件系统作为无限可恢复的上下文；通过持续复述 todo 列表操控模型注意力；在上下文中保留错误历史而非清除；在行动序列中引入多样性以打破少样本陷阱。这些原则均来自四次框架重建和面向百万用户的真实迭代。

---

**作者判断**：

作者明确认为上下文工程是 AI 代理系统的核心竞争力，而非附属技巧。原文："你如何塑造上下文最终决定了你的智能体的行为方式。" 作者将错误恢复能力视为"真正代理行为最明显的指标之一"，但指出学术基准对此严重低估。他对动态工具加载持明确反对立场，认为其同时破坏 KV 缓存和模型一致性。对于 SSM 类架构，作者提出了开放性假设：若 SSM 能掌握基于文件的外部记忆，可能成为神经图灵机的真正继任者。

---

**核心内容**：

**选择上下文工程而非微调**

BERT 时代每次迭代需要数周，GPT-3/Flan-T5 出现后上下文学习成为可行路径。Manus 押注上下文工程的动机是：改进可在数小时内交付（而非数周），产品与底层模型保持正交——"如果模型进步是上涨的潮水，我们希望 Manus 是那条船，而不是固定在海床上的柱子"。

---

**原则 1：围绕 KV 缓存设计**

Manus 代理循环中，平均输入/输出 token 比约为 **100:1**（预填充远大于解码）。KV 缓存命中与未命中的成本差距可达 **10 倍**（以 Claude Sonnet 为例：缓存 $0.30/百万 token vs 未缓存 $3/百万 token）。

![KV 缓存命中率对延迟和成本的影响示意图](https://d1oupeiobkpcny.cloudfront.net/user_upload_by_module/markdown/310708716691272617/OhdKxGRSXCcuqOvz.png)

三条具体实践：
1. **稳定提示前缀**：避免在系统提示开头放置时间戳（尤其是精确到秒），单个 token 差异即会使后续所有缓存失效
2. **上下文只追加**：不修改历史行动/观察，序列化必须确定性（JSON 键顺序不稳定会静默破坏缓存）
3. **手动标记缓存断点**：部分提供商不支持自动前缀缓存，至少确保系统提示结尾处有断点；自托管使用 vLLM 时需启用 prefix/prompt caching 并通过 session ID 路由请求到同一工作节点

---

**原则 2：遮蔽工具，而非移除工具**

工具数量爆炸（MCP 加剧）导致模型选择错误率上升。动态添加/移除工具有两个硬伤：
- 工具定义位于上下文前部，任何变更使后续全部 KV 缓存失效
- 历史行动引用已消失的工具，导致模式违规或幻觉动作

Manus 的方案：**上下文感知状态机 + logit masking**，在解码时屏蔽不可用工具的 token，而不修改工具列表。三种约束模式（以 Hermes 格式为例）：
- **自动**：预填充 `<|im_start|>assistant`
- **必需**：预填充 `<|im_start|>assistant<tool_call>`
- **指定**：预填充 `<|im_start|>assistant<tool_call>{"name": "browser_`

![状态机控制工具可用性，通过 logit masking 约束动作选择](https://d1oupeiobkpcny.cloudfront.net/user_upload_by_module/markdown/310708716691272617/cWxINCvUfrmlbvfV.png)

工具命名采用统一前缀（`browser_*`、`shell_*`），便于按前缀整组约束，无需有状态的 logits 处理器。

---

**原则 3：以文件系统作为上下文**

三个现实痛点：网页/PDF 等观察结果容易超出上下文限制；超过一定长度后模型性能下降；即使命中缓存仍需为传输和预填充每个 token 付费。

Manus 将文件系统视为终极上下文：无大小限制、天然持久化、代理可直接操作。**关键设计原则：压缩必须可恢复**——只要保留 URL，网页内容可从上下文移除；只要保留文档路径，文档内容可省略。这样缩短上下文长度而不永久丢失信息。

![文件系统作为外部记忆，代理按需读写，支持可恢复压缩](https://d1oupeiobkpcny.cloudfront.net/user_upload_by_module/markdown/310708716691272617/sBITCOxGnHNUPHTD.png)

---

**原则 4：通过复述操控注意力**

Manus 典型任务平均需要 **约 50 次工具调用**，长循环中模型容易偏离目标（"丢失在中间"问题）。

解法：让代理维护并持续更新 `todo.md`，将全局计划复述到上下文末尾，推入模型近期注意力范围。这是纯自然语言机制，无需架构改动。

![Manus 在任务执行中持续更新 todo.md，将目标复述到上下文末尾](https://d1oupeiobkpcny.cloudfront.net/user_upload_by_module/markdown/310708716691272617/OYpTzfPZaBeeWFOx.png)

---

**原则 5：保留错误，不要清除**

常见冲动是清除失败痕迹、重试或重置状态。但擦除失败即擦除证据，模型无法适应。

Manus 的做法：将失败的行动及其观察/堆栈跟踪保留在上下文中。模型看到失败后会隐式更新内部置信，降低重复同样错误的概率。作者认为**错误恢复能力是真正代理行为最明显的指标之一**，但学术基准普遍低估了这一维度。

![保留错误历史让模型更新内部置信，避免重复犯错](https://d1oupeiobkpcny.cloudfront.net/user_upload_by_module/markdown/310708716691272617/dBjZlVbKJVhjgQuF.png)

---

**原则 6：打破少样本陷阱**

语言模型是优秀的模仿者——上下文中重复的行动-观察对会诱使模型固守模式（即使不再最优）。典型案例：用 Manus 审查 20 份简历时，代理倾向于机械重复相同的处理节奏。

解法：在行动和观察中引入**受控的结构性变化**——不同的序列化模板、替代性措辞、格式上的微小噪音。上下文越单一，代理越脆弱。

![引入行动和观察的多样性，打破少样本固化模式](https://d1oupeiobkpcny.cloudfront.net/user_upload_by_module/markdown/310708716691272617/IIyBBdwwuMDJUnUc.png)
